## Bugs

- `AeroScape.Server.Network/Frames/LoginFrames.cs:72` — the login sequence never applies the Java weapon-tab setup, so the client is left on the generic attack interface (`92`) and never receives the weapon-specific tab/string/config updates that `playerWeapon.setWeapon()` sends after login; this also misses the ancient-staff magic-tab swap during login — Java reference: `legacy-java/server508/src/main/java/DavidScape/io/Login.java:289-305`, `legacy-java/server508/src/main/java/DavidScape/players/items/PlayerWeapon.java:22-121`
- `AeroScape.Server.Network/Frames/GameFrames.cs:569` — unequipping an item removes it server-side and sends frame `135`, but it never rebuilds the weapon interface afterward, unlike Java’s `removeEquipment`, which immediately calls `PlayerWeapon.setWeapon()` to resend the correct attack-tab interface/string/config state — Java reference: `legacy-java/server508/src/main/java/DavidScape/io/Frames.java:586-616`, `legacy-java/server508/src/main/java/DavidScape/players/items/PlayerWeapon.java:22-121`
- `AeroScape.Server.Network/Frames/GameFrames.cs:695` — `SetInterfaces` writes `AutoCastSpellId = 1` when weapon `4675` is equipped, but the Java code sets the spellbook flag (`isAncients = 1`) instead; this can force an unintended autocast selection instead of just switching the client to the ancient-magic interface (`193`) — Java reference: `legacy-java/server508/src/main/java/DavidScape/io/Frames.java:760-765`, `legacy-java/server508/src/main/java/DavidScape/io/Frames.java:782-786`
