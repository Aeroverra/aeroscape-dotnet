## Bugs
- `AeroScape.Server.Core/Engine/GameEngine.cs:395` — `ProcessGlobalTimers()` never validates `ZamorakP` / `SaradominP`, so Castle Wars flags stay marked as taken when the carrier logs out or disappears; Java clears the carrier slot and resets the flag booleans each tick. — Java reference: `legacy-java/server508/src/main/java/DavidScape/Engine.java:342-357`
- `AeroScape.Server.Core/Engine/GameEngine.cs:609` — the home-teleport sequence never runs. `MagicService` still sets `HomeTele = 15` and `NormalHomeTele = true`, but the tick only decrements `HomeTeleDelay`; it never calls `homeTeleport(...)`, never advances the animation/gfx chain, and never performs the final teleport. — Java reference: `legacy-java/server508/src/main/java/DavidScape/players/Player.java:1381-1434`, `legacy-java/server508/src/main/java/DavidScape/players/Player.java:3964-3973`
- `AeroScape.Server.Core/Engine/GameEngine.cs:679` — skull updates are applied unconditionally, so duel-area players can have `PkIcon` changed/cleared by the normal skull timer. Java explicitly suppresses this block inside the Duel Arena. — Java reference: `legacy-java/server508/src/main/java/DavidScape/players/Player.java:4317-4327`
- `AeroScape.Server.Core/Services/DeathService.cs:23` — player death no longer executes the Java `applyDead()` side effects before restore: no death message/animation, no retribution burst, no wilderness item drops or gravestone handling, no familiar cleanup, no bounty removal, and no follow/overlay cleanup. The C# path just counts down and restores stats/energy. — Java reference: `legacy-java/server508/src/main/java/DavidScape/players/Player.java:4438-4602`
- `AeroScape.Server.Core/Services/DeathService.cs:48` — all player deaths respawn to `(3222, 3219, 0)`. Java uses area-specific death handling for Castle Wars, Fight Pits, clan wars, and duel resolution before falling back to home respawn. — Java reference: `legacy-java/server508/src/main/java/DavidScape/players/Player.java:4536-4602`
- `AeroScape.Server.Core/Handlers/NPCAttackMessageHandler.cs:49` — attacking an NPC no longer sets the NPC’s `followPlayer`, so melee NPCs do not keep chasing the attacker after the initial click. Java sets `n.followPlayer = playerId` as part of the attack packet handling. — Java reference: `legacy-java/server508/src/main/java/DavidScape/io/packets/NPCAttack.java:36-38`
- `AeroScape.Server.Core/Entities/NPC.cs:222` — `NPC.Process()` only follows `Owner`, not the indexed `FollowPlayer`. Even if `FollowPlayer` is populated, non-summoned hostile NPCs never run the legacy `appendPlayerFollowing(...)` path, so they lose the Java chase behavior. — Java reference: `legacy-java/server508/src/main/java/DavidScape/npcs/NPC.java:360-379`
- `AeroScape.Server.Core/Entities/NPC.cs:234` — `RespawnDelay` is decremented in `NPC.Process()` and then decremented again in `DeathService.ProcessNpcDeath()` once the NPC is hidden, which shortens NPC respawn timers versus Java. The legacy server only decrements `respawnDelay` once per tick in `NPC.process()`. — Java reference: `legacy-java/server508/src/main/java/DavidScape/npcs/NPC.java:371-372`, `legacy-java/server508/src/main/java/DavidScape/Engine.java:408-427`
